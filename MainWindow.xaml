<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    lex:LocalizeDictionary.DesignCulture="zh-CN"
    lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
    lex:ResxLocalizationProvider.DefaultDictionary="Langs"
    x:Name="TheWindow" x:Class="MajdataEdit.MainWindow"
    mc:Ignorable="d"
    Title="MajdataEdit" Height="589" Width="720.5" Loaded="Window_Loaded" Closing="Window_Closing" MinHeight="517"
    WindowStartupLocation="CenterScreen">

    <Grid DragEnter="Grid_DragEnter" AllowDrop="True" Drop="Grid_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF526675" />
                <GradientStop Color="#FF735573" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- 顶部菜单栏 -->
        <DockPanel Grid.Row="0" Height="19" Background="{DynamicResource ButtonsBackground}">
            <Menu DockPanel.Dock="Right" HorizontalAlignment="Right" Background="{DynamicResource ButtonsBackground}"
                  Foreground="{DynamicResource ButtonForeground}">
                <MenuItem Header="Majnet" Click="MenuItem_Majnet_Click"
                          Foreground="{DynamicResource ButtonForeground}" Template="{DynamicResource DarkMenuItem}" />
                <MenuItem Header="{lex:Loc Github}" Click="MenuItem_GitHub_Click"
                          Foreground="{DynamicResource ButtonForeground}" Template="{DynamicResource DarkMenuItem}" />
                <MenuItem Header="{lex:Loc CheckUpdate}" Click="CheckUpdate_Click"
                          Foreground="{DynamicResource ButtonForeground}" Template="{DynamicResource DarkMenuItem}" />
            </Menu>
            <Menu Background="{DynamicResource ButtonsBackground}" Foreground="{DynamicResource ButtonForeground}">
                <MenuItem Header="{lex:Loc Files}" Foreground="{DynamicResource ButtonForeground}"
                          Template="{DynamicResource DarkMenuItem}">
                    <MenuItem x:Name="Menu_New" Header="{lex:Loc New}" Click="Menu_New_Click" />
                    <MenuItem x:Name="Menu_Open" Header="{lex:Loc Open}" Click="Menu_Open_Click" />
                    <MenuItem x:Name="Menu_Save" Header="{lex:Loc Save}" Click="Menu_Save_Click" />
                    <MenuItem x:Name="Menu_SaveAs" Header="{lex:Loc SaveAs}" Click="Menu_SaveAs_Click" IsEnabled="False"
                              Visibility="Collapsed" />
                    <Separator />
                    <MenuItem x:Name="Menu_AutosaveRecover" Header="{lex:Loc AutosaveRecoverWindowTitle}"
                              Click="Menu_AutosaveRecover_Click" />
                    <Separator />
                    <MenuItem x:Name="Menu_ExportRender" Header="{lex:Loc ExportVideo}" Click="Menu_ExportRender_Click"
                              IsEnabled="False" />

                </MenuItem>
                <MenuItem x:Name="MenuEdit" Header="{lex:Loc Edit}" IsEnabled="False"
                          Foreground="{DynamicResource ButtonForeground}" Template="{DynamicResource DarkMenuItem}">
                    <MenuItem Header="{lex:Loc MirrorLR}" Click="MirrorLeftRight_MenuItem_Click" />
                    <MenuItem Header="{lex:Loc MirrorUD}" Click="MirrorUpDown_MenuItem_Click" />
                    <MenuItem Header="{lex:Loc Mirror180}" Click="Mirror180_MenuItem_Click" />
                    <MenuItem Header="{lex:Loc Mirror45}" Click="Mirror45_MenuItem_Click" />
                    <MenuItem Header="{lex:Loc MirrorCcw45}" Click="MirrorCcw45_MenuItem_Click" />
                    <Separator />
                    <MenuItem Header="{lex:Loc MapInfo}" Click="MenuItem_InfomationEdit_Click" />
                    <Separator />
                    <MenuItem Header="{lex:Loc FindAndReplace}" Click="MenuFind_Click" />
                </MenuItem>
                <MenuItem Header="{lex:Loc Setting}" Foreground="{DynamicResource ButtonForeground}"
                          Template="{DynamicResource DarkMenuItem}">
                    <MenuItem x:Name="VolumnSetting" Header="{lex:Loc Volume}" Click="MenuItem_SoundSetting_Click"
                              IsEnabled="False" />
                    <Separator />
                    <MenuItem Header="{lex:Loc EditorSetting}" Click="MenuItem_EditorSetting_Click" />
                    <MenuItem Header="{lex:Loc RemapViewer}" Click="Menu_ResetViewWindow" />
                </MenuItem>
                <MenuItem Header="{lex:Loc Tools}" Foreground="{DynamicResource ButtonForeground}"
                          Template="{DynamicResource DarkMenuItem}">
                    <MenuItem Header="{lex:Loc BPMtap}" Click="BPMtap_MenuItem_Click" />
                    <MenuItem Header="从osu文件获取Timing以及HitObjects" Click="ImportTimingFromOsuBeatmap_MenuItem_Click" />
                    <MenuItem x:Name="MenuMuriCheck" Header="{lex:Loc MapMuriDetc}" Click="MuriCheck_Click_1"
                              IsEnabled="False" />
                    <MenuItem x:Name="SyntaxCheckButton" Header="{lex:Loc SyntaxDisplay}" Click="SyntaxCheckButton_Click"
                              IsEnabled="False" />
                </MenuItem>
                <MenuItem x:Name="MenuGenerate" Header="生成" Foreground="{DynamicResource ButtonForeground}"
                          Template="{DynamicResource DarkMenuItem}">
                    <MenuItem x:Name="Menu_GenerateMa2" Header=".ma2谱面文件" Click="Menu_GenerateMa2_Click" />
                    <MenuItem x:Name="Menu_ExportCoverVideo"
                              Header="{lex:Loc ExportCoverAsVideo}"
                              Click="Menu_ExportCoverVideo_Click" />
                </MenuItem>
            </Menu>
        </DockPanel>

        <!-- FFT 频谱：须声明在主体编辑区之前，保持从 FumenContent 半透明背景下方透出的现有层级 -->
        <Image x:Name="FFTImage" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Width="254" Height="255" Margin="0,0,14,17" RenderTransformOrigin="0.5,0.5">
            <Image.RenderTransform>
                <RotateTransform Angle="-90" />
            </Image.RenderTransform>
        </Image>

        <!-- 主体区：左侧边栏 + 谱面编辑器 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 侧边栏：顶部信息组自上而下排列，底部操作组自下而上锚定（窗口极矮时两组重叠，与旧布局行为一致） -->
            <Grid Grid.Column="0">
                <Grid.Resources>
                    <Style TargetType="Label">
                        <Setter Property="Foreground" Value="White" />
                    </Style>
                </Grid.Resources>
                <StackPanel VerticalAlignment="Top">
                    <Label x:Name="TimeLabel" Content="0:00" Height="64" FontFamily="Bahnschrift Light" FontSize="50"
                           VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                    <!-- 负上边距：保持与计时器数字原有的紧凑叠放 -->
                    <Label x:Name="NoteNowText" Content="行" Height="25" Margin="0,-7,0,0"
                           HorizontalContentAlignment="Center" />
                    <ComboBox x:Name="LevelSelector" Margin="5" HorizontalAlignment="Stretch"
                              SelectionChanged="ComboBox_SelectionChanged"
                              Background="{DynamicResource ButtonsBackground}"
                              Foreground="{DynamicResource ButtonForeground}"
                              Template="{DynamicResource DarkComboBox}">

                        <ComboBoxItem Content="EASY" />
                        <ComboBoxItem Content="BASIC" />
                        <ComboBoxItem Content="ADVANCED" />
                        <ComboBoxItem Content="EXPERT" />
                        <ComboBoxItem Content="MASTER" />
                        <ComboBoxItem Content="Re:MASTER" />
                        <ComboBoxItem Content="ORIGINAL" />
                    </ComboBox>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="{lex:Loc Difficulty}"  Height="25" />
                        <TextBox Grid.Column="1" x:Name="LevelTextBox" Width="46" Height="25" TextWrapping="Wrap"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Right"
                                 TextChanged="LevelTextBox_TextChanged"
                                 Background="{DynamicResource ButtonsBackground}"
                                 Foreground="{DynamicResource ButtonForeground}" />
                    </Grid>
                    <Grid  Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="{lex:Loc Offset}" Height="25" />
                        <TextBox Grid.Column="1" x:Name="OffsetTextBox" Width="46" Height="25" TextWrapping="Wrap"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Right"
                                 TextChanged="OffsetTextBox_TextChanged" MouseWheel="OffsetTextBox_MouseWheel"
                                 Background="{DynamicResource ButtonsBackground}"
                                 Foreground="{DynamicResource ButtonForeground}" />
                    </Grid>
                    <Grid Margin="5">
                        <Label x:Name="ErrCount_Label" Content="Error"  HorizontalContentAlignment="Left"
                               PreviewMouseDoubleClick="SyntaxCheckButton_Click" />
                        <Label x:Name="ErrCount" Content="0"  Height="25" HorizontalContentAlignment="Right"
                               PreviewMouseDoubleClick="SyntaxCheckButton_Click" />
                    </Grid>
                </StackPanel>
                <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,16">
                    <Grid Height="24" Margin="5,0,0,0">
                        <Label Content="{lex:Loc Brightness}"  HorizontalAlignment="Left"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                        <Label x:Name="ViewerCover" Content="0.6" Width="35" Height="24" Margin="60,0,0,0"
                               HorizontalAlignment="Left" VerticalContentAlignment="Center"
                               HorizontalContentAlignment="Center" Foreground="{DynamicResource ButtonForeground}"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                    </Grid>
                    <Grid Height="24" Margin="5,0,0,0">
                        <Label Content="{lex:Loc Speed}"  HorizontalAlignment="Left"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                        <Label x:Name="ViewerSpeed" Content="7.0" Width="35" Height="24" Margin="60,0,0,0"
                               HorizontalAlignment="Left" VerticalContentAlignment="Center"
                               HorizontalContentAlignment="Center" Foreground="{DynamicResource ButtonForeground}"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                    </Grid>
                    <!-- TouchSpeed 标签宽 80，故意垫在 x=60 处的数值标签之下：长本地化文本时数值（后声明）仍渲染在上层 -->
                    <Grid Height="24" Margin="5,2,0,0">
                        <Label Content="{lex:Loc TouchSpeed}"  Height="24" HorizontalAlignment="Left"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                        <Label x:Name="ViewerTouchSpeed" Content="7.0"  Height="24" Margin="60,0,0,0"
                               HorizontalAlignment="Left" VerticalContentAlignment="Center"
                               HorizontalContentAlignment="Right" Foreground="{DynamicResource ButtonForeground}"
                               MouseUp="SettingLabel_MouseUp" ToolTip="{lex:Loc SpeedSettingTooltip}" />
                    </Grid>
                    <Button x:Name="Op_Button" Content="{lex:Loc RecMode}"  Height="30" Margin="9,5,0,0"
                            HorizontalAlignment="Stretch" Click="Op_Button_Click"
                            Template="{DynamicResource DarkButton}"
                            Foreground="{DynamicResource ButtonForeground}"
                            Background="{DynamicResource ButtonsBackground}"
                            ToolTip="{lex:Loc RecordModeDiscrp}" />
                    <StackPanel x:Name="WaveOptionChecks" Margin="5" HorizontalAlignment="Left">
                        <CheckBox x:Name="FollowPlayCheck" Content="{lex:Loc TextFollow}" FontSize="11"
                                  Foreground="{DynamicResource ButtonForeground}" HorizontalContentAlignment="Right"
                                  Click="FollowPlayCheck_Click" Background="{DynamicResource ButtonsBackground}"
                                  Template="{DynamicResource DarkCheck}" />
                        <CheckBox x:Name="DrawHSpeedChangesCheck" Content="{lex:Loc DrawHSpeedChanges}" FontSize="11"
                                  Foreground="{DynamicResource ButtonForeground}" HorizontalContentAlignment="Right"
                                  Checked="DrawHSpeedChangesCheck_CheckedChanged"
                                  Unchecked="DrawHSpeedChangesCheck_CheckedChanged"
                                  Background="{DynamicResource ButtonsBackground}"
                                  Template="{DynamicResource DarkCheck}" />
                        <CheckBox x:Name="ColorObjectsByHSpeedGroupCheck" FontSize="11"
                                  Foreground="{DynamicResource ButtonForeground}" HorizontalContentAlignment="Right"
                                  ToolTip="{lex:Loc ColorObjectsByHSpeedGroup}"
                                  Checked="ColorObjectsByHSpeedGroupCheck_CheckedChanged"
                                  Unchecked="ColorObjectsByHSpeedGroupCheck_CheckedChanged"
                                  Background="{DynamicResource ButtonsBackground}"
                                  Template="{DynamicResource DarkCheck}">
                            <TextBlock Text="{lex:Loc ColorObjectsByHSpeedGroup}" TextAlignment="Right"
                                       TextTrimming="CharacterEllipsis" />
                        </CheckBox>
                        <CheckBox x:Name="AutoPreviewUpdateCheck" Content="自动更新预览" FontSize="11"
                                  Foreground="{DynamicResource ButtonForeground}" HorizontalContentAlignment="Right"
                                  Checked="AutoPreviewUpdateCheck_CheckedChanged"
                                  Unchecked="AutoPreviewUpdateCheck_CheckedChanged"
                                  Background="{DynamicResource ButtonsBackground}"
                                  Template="{DynamicResource DarkCheck}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,1,0,0">
                        <Button x:Name="StopButton" Content="◼" Width="38" Height="38" Margin="9,0,0,0"
                                Click="StopButton_Click"
                                Background="{DynamicResource ButtonsBackground}"
                                Foreground="{DynamicResource ButtonForeground}"
                                Template="{DynamicResource DarkButton}" />
                        <Button x:Name="PlayAndPauseButton" Content="▶" Width="38" Height="38" Margin="11,0,0,0"
                                Click="PlayAndPauseButton_Click"
                                Background="{DynamicResource ButtonsBackground}"
                                Foreground="{DynamicResource ButtonForeground}"
                                Template="{DynamicResource DarkButton}" />
                    </StackPanel>
                </StackPanel>
            </Grid>

            <RichTextBox x:Name="FumenContent" Grid.Column="1" Margin="0,10,10,16"
                         Padding="0,5" FontFamily="Consolas"
                         Foreground="#FFE8E8E8" TextChanged="FumenContent_TextChanged"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Visible"
                         SelectionChanged="FumenContent_SelectionChanged"
                         InputMethod.IsInputMethodEnabled="False"
                         AutoWordSelection="False"
                         PreviewMouseRightButtonDown="FumenContent_OnPreviewMouseRightButtonDown"
                         PreviewKeyDown="FumenContent_OnPreviewKeyDown">
                <RichTextBox.Resources>
                    <RoutedUICommand x:Key="PlayAndPause" Text="PlayAndPause" />
                    <RoutedUICommand x:Key="SaveFile" Text="SaveFile" />
                    <RoutedUICommand x:Key="StopPlaying" Text="StopPlaying" />
                    <RoutedUICommand x:Key="SendToView" Text="SendToView" />
                    <RoutedUICommand x:Key="IncreasePlaybackSpeed" Text="IncreasePlaybackSpeed" />
                    <RoutedUICommand x:Key="DecreasePlaybackSpeed" Text="DecreasePlaybackSpeed" />
                    <RoutedUICommand x:Key="Find" Text="Find" />
                    <RoutedUICommand x:Key="MirrorLR" Text="MirrorLR" />
                    <RoutedUICommand x:Key="MirrorUD" Text="MirrorUD" />
                    <RoutedUICommand x:Key="Mirror180" Text="Mirror180" />
                    <RoutedUICommand x:Key="Mirror45" Text="Mirror45" />
                    <RoutedUICommand x:Key="MirrorCcw45" Text="MirrorCcw45" />
                </RichTextBox.Resources>
                <RichTextBox.Background>
                    <SolidColorBrush Color="#FF171717" Opacity="0.3" />
                </RichTextBox.Background>
                <RichTextBox.ContextMenu>
                    <ContextMenu Opened="FumenContentContextMenu_OnOpened"
                                 Closed="FumenContentContextMenu_OnClosed">
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuUndo}"
                                  Command="ApplicationCommands.Undo"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuRedo}"
                                  Command="ApplicationCommands.Redo"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <Separator />
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuCut}"
                                  Command="ApplicationCommands.Cut"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuCopy}"
                                  Command="ApplicationCommands.Copy"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuPaste}"
                                  Command="ApplicationCommands.Paste"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <MenuItem Header="{lex:Loc Key=EditorContextMenuSelectAll}"
                                  Command="ApplicationCommands.SelectAll"
                                  CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        <Separator />
                        <MenuItem x:Name="FumenContentJumpToCursorMenuItem"
                                  Header="{lex:Loc Key=EditorContextMenuJumpToCursor}"
                                  Click="FumenContentJumpToCursorMenuItem_Click" />
                    </ContextMenu>
                </RichTextBox.ContextMenu>
                <!--Bindings are dynamicly added via ReadEditorSetting()-->
                <RichTextBox.CommandBindings>
                    <CommandBinding Command="{StaticResource PlayAndPause}" CanExecute="PlayAndPause_CanExecute" />
                    <CommandBinding Command="{StaticResource SaveFile}" CanExecute="SaveFile_Command_CanExecute" />
                    <CommandBinding Command="{StaticResource StopPlaying}" CanExecute="StopPlaying_CanExecute" />
                    <CommandBinding Command="{StaticResource SendToView}" CanExecute="SendToView_CanExecute" />
                    <CommandBinding Command="{StaticResource IncreasePlaybackSpeed}"
                                    CanExecute="IncreasePlaybackSpeed_CanExecute" />
                    <CommandBinding Command="{StaticResource DecreasePlaybackSpeed}"
                                    CanExecute="DecreasePlaybackSpeed_CanExecute" />
                    <CommandBinding Command="{StaticResource Find}" CanExecute="FindCommand_CanExecute" />
                    <CommandBinding Command="{StaticResource MirrorLR}" CanExecute="MirrorLRCommand_CanExecute" />
                    <CommandBinding Command="{StaticResource MirrorUD}" CanExecute="MirrorUDCommand_CanExecute" />
                    <CommandBinding Command="{StaticResource Mirror180}" CanExecute="Mirror180Command_CanExecute" />
                    <CommandBinding Command="{StaticResource Mirror45}" CanExecute="Mirror45Command_CanExecute" />
                    <CommandBinding Command="{StaticResource MirrorCcw45}" CanExecute="MirrorCcw45Command_CanExecute" />

                </RichTextBox.CommandBindings>
                <FlowDocument />
            </RichTextBox>
        </Grid>

        <!-- 底部波形条：占满窗口全宽（InitWave/ScrollWave 以 Window.Width 为基准） -->
        <Grid Grid.Row="2">
            <Image x:Name="MusicWave" Height="74" MouseWheel="MusicWave_MouseWheel"
                   PreviewMouseLeftButtonDown="MusicWave_PreviewMouseLeftButtonDown" MouseMove="MusicWave_MouseMove"
                   SizeChanged="MusicWave_SizeChanged" />
            <!-- X1/X2=22.5：Line 默认沿元素左缘描线，须偏移到 45 宽元素的中点，红色光标才对准窗口中心（= ScrollWave 的 Width/2 假设） -->
            <Line StrokeThickness="3" Stroke="Red" X1="22.5" Y1="15" X2="22.5" Y2="56"
                  Width="45" Height="74" HorizontalAlignment="Center" />
            <Button x:Name="WaveViewZoomIn" Content="+" Width="24" Height="24" Margin="10,0,0,40"
                    HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="WaveViewZoomIn_Click"
                    Background="{DynamicResource ButtonsBackground}" Foreground="{DynamicResource ButtonForeground}"
                    Template="{DynamicResource DarkButton}" />
            <Button x:Name="WaveViewZoomOut" Content="-" Width="24" Height="24" Margin="10,0,0,9"
                    HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="WaveViewZoomOut_Click"
                    Background="{DynamicResource ButtonsBackground}" Foreground="{DynamicResource ButtonForeground}"
                    Template="{DynamicResource DarkButton}" />
        </Grid>

        <!-- 以下为运行时切换 Visibility 的浮层，声明顺序即叠放顺序（Cover 最后 = 最顶层） -->
        <Grid x:Name="PlbSpdAdjGrid" Grid.Row="1" Width="148" Height="105" Margin="105,270,0,0"
              HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Hidden">
            <Grid.Background>
                <SolidColorBrush Color="#FF303030" Opacity="0.8" />
            </Grid.Background>
            <Label Content="{lex:Loc PlayBackSpeed}" Height="26" Margin="44,20,44,0" VerticalAlignment="Top"
                   Foreground="White" HorizontalContentAlignment="Center" />
            <Label x:Name="PlbSpdLabel" Content="100%" Margin="34,46,34,20" Foreground="White" FontWeight="Bold"
                   FontSize="22" HorizontalContentAlignment="Center" />
        </Grid>
        <Grid x:Name="FindGrid" Grid.Row="1" Width="272" Height="69" Margin="0,10,28,0"
              HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Collapsed">
            <Grid.Background>
                <SolidColorBrush Color="Black" Opacity="0.2" />
            </Grid.Background>
            <TextBox x:Name="InputText" HorizontalAlignment="Left" Height="22" Margin="10,10,0,0" TextWrapping="Wrap"
                     VerticalAlignment="Top" Width="231" VerticalContentAlignment="Center" />
            <Image x:Name="Find_icon" HorizontalAlignment="Left" Height="22" Margin="246,10,0,0"
                   VerticalAlignment="Top" Width="26" Source="Image/outline_search_white_24dp.png"
                   MouseDown="Find_icon_MouseDown" ToolTip="{lex:Loc FindNext}" />
            <Image x:Name="Replace_icon" HorizontalAlignment="Left" Height="22" Margin="246,37,0,0"
                   VerticalAlignment="Top" Width="26" Source="Image/outline_find_replace_white_24dp.png"
                   MouseDown="Replace_icon_MouseDown" ToolTip="{lex:Loc ReplaceNext}" />
            <Label x:Name="FindClose" Content="^" Height="19" Margin="127,53,127,-3" VerticalAlignment="Top"
                   FontSize="11" FontWeight="Bold" Foreground="White" MouseDown="FindClose_MouseDown" />
            <TextBox x:Name="ReplaceText" HorizontalAlignment="Left" Height="22" Margin="10,37,0,0" TextWrapping="Wrap"
                     VerticalAlignment="Top" Width="231" VerticalContentAlignment="Center" />
        </Grid>
        <StackPanel x:Name="OverrideModeTipsPopup" Grid.Row="1" Margin="105,10,0,0" HorizontalAlignment="Left"
                    VerticalAlignment="Top" Orientation="Horizontal" Visibility="Collapsed" Height="0" Width="0">
            <StackPanel.Background>
                <SolidColorBrush Color="Black" Opacity="0.8" />
            </StackPanel.Background>
            <Label Content="{lex:Loc OverwriteMode}" Foreground="White" />
            <Label Foreground="{DynamicResource HelperForeground}" Margin="3,0,0,0">
                <Label.ToolTip>
                    <ToolTip>
                        <StackPanel>
                            <TextBlock Text="{lex:Loc Key=OverwriteModeTooltip}" />
                        </StackPanel>
                    </ToolTip>
                </Label.ToolTip>
                <TextBlock TextDecorations="Underline" FontSize="10" Text="{lex:Loc Key=WhatsThis}" />
            </Label>
        </StackPanel>
        <Rectangle x:Name="Cover" Grid.RowSpan="3" Fill="#FF1F1F1F" Margin="0,18,0,0" Stroke="Black"
                   d:IsHidden="True" />
    </Grid>
</Window>
